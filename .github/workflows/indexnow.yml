name: Submit URLs to IndexNow
on:
  push:
    branches: [ main ]
  schedule:
    - cron: '17 6 * * *'
  workflow_dispatch:
jobs:
  indexnow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 .github/scripts/indexnow_submit.py

